defaults:
- ../fiveg_main.yaml
name: adv5g_0p01
description: Adversarial weight 0.01 on 5G Traffic, where the capture-session nuisance
  is genuinely decodable from the raw features.
model:
  adversarial_head: true
loss:
  lambda_adversarial: 0.01
eval:
  probes: true
  early_classification: false
  robustness: false
