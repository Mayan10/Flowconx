defaults:
- ../cesnet_main.yaml
name: ablation_no_dual_encoder
description: One shared encoder over the packet sequence with the flow context appended
  to every position.
model:
  dual_encoder: false
  fusion: none
