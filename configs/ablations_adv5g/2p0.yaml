defaults:
- ../fiveg_main.yaml
name: adv5g_2p0
description: Adversarial weight 2.0 on 5G Traffic, where the capture-session nuisance
  is genuinely decodable from the raw features.
model:
  adversarial_head: true
loss:
  lambda_adversarial: 2.0
eval:
  probes: true
  early_classification: false
  robustness: false
