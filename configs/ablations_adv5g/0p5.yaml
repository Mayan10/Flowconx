defaults:
- ../fiveg_main.yaml
name: adv5g_0p5
description: Adversarial weight 0.5 on 5G Traffic, where the capture-session nuisance
  is genuinely decodable from the raw features.
model:
  adversarial_head: true
loss:
  lambda_adversarial: 0.5
eval:
  probes: true
  early_classification: false
  robustness: false
