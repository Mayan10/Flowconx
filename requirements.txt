# Runtime dependencies with upper bounds, so a major release of any of these
# cannot silently change a published number. The exact versions a given result
# was produced with are recorded in that run's metrics.json under
# provenance.libraries -- these are the range the code is known to work in.
numpy>=1.26,<3
pandas>=2.1,<3
torch>=2.2
scikit-learn>=1.4,<2
scipy>=1.11,<2
pyyaml>=6.0,<7
matplotlib>=3.8,<4
xgboost>=2.0,<3
